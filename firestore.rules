rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Default Deny: All read and write operations are denied by default.
    // Explicit authorization rules will be introduced in future feature modules.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
